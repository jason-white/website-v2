[functions]
  directory = "src/functions"
  node_bundler = "esbuild"

[build]
  command = "npm run prod"
  publish = "dist"
[[plugins]]
package = "netlify-plugin-cache"

  [plugins.inputs]
  paths = [
    ".cache"
  ]

[dev]
  autoLaunch = false
